[build-system]
requires = [
  "scikit-build-core>=0.10",
  "nanobind>=2.0",
]
build-backend = "scikit_build_core.build"

[project]
name = "fvmc"
version = "2.0.0"
description = "A research codebase for Fock-space variational Monte Carlo"
readme = "README.md"
requires-python = ">=3.12"
license = "MIT"
license-files = ["LICENSE"]

authors = [
  { name = "Zheng (Alex) Che", email = "wsmxcz@gmail.com" },
]

dependencies = [
  "flax>=0.12",
  "jax[cuda12]>=0.10",
  "matplotlib>=3.8",
  "numpy>=2.4",
  "optax>=0.2",
  "pyscf>=2.13",
  "scipy>=1.10",
]

[project.optional-dependencies]
dev = [
  "pytest",
]

[tool.scikit-build]
cmake.version = ">=3.20"
cmake.build-type = "Release"

wheel.packages = [
  "fvmc",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-ra"
